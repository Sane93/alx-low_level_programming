#include "main.h"
#include <stdio.h>
/**
 * main- print name of a program
 * @argc: number of names to printed
 * @argv: name of the program
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
